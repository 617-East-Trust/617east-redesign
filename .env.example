# 617 East Trust — Environment Variables
# Copy this file to .env (production) or reference when deploying.
# Never commit actual secrets.

# ── Analytics ──────────────────────────────────────────────────────────────────
# Umami (privacy-first analytics — self-hosted recommended).
# Set both to enable runtime injection.
ANALYTICS_ENDPOINT=https://analytics.example.com
ANALYTICS_WEBSITE_ID=your-public-umami-website-id

# Google Analytics 4 — ID only. The actual gtag script is injected by
# CookieConsent.tsx only after the visitor clicks "Accept".
GA4_ID=

# Microsoft Clarity — Project ID only. The clarity.js tag is injected by
# CookieConsent.tsx only after the visitor clicks "Accept".
CLARITY_ID=

# ── Privacy / Consent ─────────────────────────────────────────────────────────
# Optional: POST webhook that receives a JSON event on each consent decision
# (accepted / declined), with fields: { event, visitorId, route, timestamp, consentVersion }
# Leave blank to disable server-side consent logging.
CONSENT_RECORDER_URL=

# ── Server ─────────────────────────────────────────────────────────────────────
PORT=3000
NODE_ENV=production
