services:
  app:
    build: .
    container_name: 617east-web
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - ANALYTICS_ENDPOINT
      - ANALYTICS_WEBSITE_ID
    restart: unless-stopped